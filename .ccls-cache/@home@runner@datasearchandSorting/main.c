#include <stdio.h>
#include "header.h"

int main() {
  
  // int array[] = {64, 34, 25, 12, 22, 11, 90};
  // int n = sizeof(array) / sizeof(array[0]);
  // bubbleSort(array, n);
  // printf("Array ordenado: \n");
  // for (int i = 0; i < n; i++)
  // printf("%d ", array[i]);
  
  lerTxt();
  
  return 0;
}